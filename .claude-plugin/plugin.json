{
  "name": "mnemo",
  "description": "Per-project persistent memory: plain-text notes versioned in git, syncable across your machines. Ships an MCP server, so the same memory is reachable from any MCP-capable agent. Commands: /mnemo:load-context, /mnemo:save-context, /mnemo:mem, /mnemo:list-context, /mnemo:rename, /mnemo:forget.",
  "version": "0.3.0",
  "author": {
    "name": "Alexander Parco"
  },
  "homepage": "https://github.com/AlexParco/mnemo",
  "repository": "https://github.com/AlexParco/mnemo",
  "license": "MIT",
  "keywords": [
    "memory",
    "context",
    "persistence",
    "notes",
    "git"
  ],
  "mcpServers": {
    "mnemo": {
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/mnemo-mcp.sh"
    }
  }
}
